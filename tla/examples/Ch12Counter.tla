----------------------------- MODULE Ch12Counter -----------------------------
\* 第 12 章配套**库模块**：一个被参数化的「环形计数器」纯算子模块。
\* 它没有 .cfg，不会被 run-all.sh 单独运行——它的存在是为了被 Ch12ModulesInstance
\* 用 INSTANCE ... WITH 以不同参数实例化两次，演示「同一份泛型规格，喂不同常量」。
\*
\* 参数化的关键：把会变的东西声明成 CONSTANT，由「使用方」在 INSTANCE 时填值。

EXTENDS Integers

CONSTANT Max                 \* 计数上限，由实例化方提供

InitVal == 0                 \* 起点值

\* 一步：没到 Max 就加一，到 Max 就归零（环形）。注意这是**纯函数**，
\* 输入当前值、输出下一个值，不涉及带撇变量——这样最容易被复用。
StepVal(v) == IF v < Max THEN v + 1 ELSE 0

\* 值是否在合法区间内
InRange(v) == v \in 0..Max
=============================================================================
