--------------------------- MODULE Ch07SafetyLiveness ---------------------------
\* 第 07 章：安全性 vs 活性——并发/分布式系统里两类最重要的性质。
\*
\*   安全性（safety）：坏事**永远不**发生。形式：[](\lnot Bad) 或某个不变式。
\*       「灯只会是红或绿，绝不会是蓝」——只要找到一个状态违反就证伪。
\*   活性（liveness）：好事**终将**发生。形式：<>Good 或 []<>Good。
\*       「灯会无限次变绿」——必须看完整条无限行为才能判定，需要公平性。
\*
\* 用一个红绿灯演示：它在红/绿之间不停翻转。

EXTENDS TLC

VARIABLES light
vars == <<light>>

Init == light = "red"

\* 每步翻转颜色（红->绿->红->...）。这是个「永远使能」的动作。
Flip == light' = IF light = "red" THEN "green" ELSE "red"

Next == Flip

\* 弱公平保证 Flip 不会无限期停摆，于是灯真的一直在变。
Spec == Init /\ [][Next]_vars /\ WF_vars(Next)

\* --- 安全性：灯永远是红或绿，绝不会出现别的颜色 ---------------------
SafetyNeverBlue == [](light \in {"red", "green"})

\* --- 活性：灯会无限次变绿（[]<> 读作 infinitely often）-------------
LivenessGreenIO == []<>(light = "green")

\* --- 活性：也会无限次变红 ------------------------------------------
LivenessRedIO   == []<>(light = "red")

\* --- 一个更强的活性：从任意时刻起，下一步之内必变绿 ----------------
\* 因为红之后必是绿，绿之后必是红，所以「无限次绿」成立，
\* 但「每个状态之后立刻是绿」不成立（绿之后是红）。下面这条**故意不查**，
\* 留作思考：[](light = "green") 显然为假。
NotChecked == [](light = "green")
=============================================================================
