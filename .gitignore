# =========================
# Guide-wide ignore rules
# =========================

# OS / editor
.DS_Store
Thumbs.db
Desktop.ini
*.swp
*.swo
*.tmp
*.log

.vscode/
.vs/
**/.idea/
.claude/

# Generic build output folders used across guides
**/build/
**/bin/
**/obj/
**/out/
**/dist/
**/target/

# Language/tool specific caches
**/.dart_tool/
**/.packages
**/pubspec.lock
**/.zig-cache/
**/zig-out/
**/.lake/build/
**/.lake/packages/
*.fasl

# Zig Windows binary artifact
**/$outExe

# C/C++/MSVC artifacts
*.obj
*.o
*.pdb
*.ilk
*.idb
*.pch
*.lib
*.exp
*.dll
*.exe

# Free Pascal / Lazarus artifacts
# .ppu 是 FPC 单元产物，.compiled 是 Lazarus 记录的构建元数据。
# lazbuild 还按「目标CPU-目标OS」把单元写进每个工程自己的 lib/ 目录
# （x86_64-win64 / x86_64-darwin / aarch64-darwin …），目录名随平台变，故整目录忽略。
# 不能写成 **/lib/：flutter、dart、asm 这些目录下的 lib/ 是源码。
*.ppu
*.compiled
freepascal/examples/*/lib/
# selftest 运行时产物（GUI 示例的 --selftest 日志）
freepascal/examples/*/selftest.log

# .NET artifacts
*.user
*.rsuser
*.suo
*.cache
*.deps.json
*.runtimeconfig.json

# Gradle / Android 工程缓存（.gradle 是 Gradle 守护进程内部状态，
# local.properties 含本机 SDK 路径——都不该进仓库）
**/.gradle/
**/local.properties

# D 语言 DUB 构建缓存（dub.json 工程目录下的本地产物）
**/.dub/
*.lst

# Swift Package Manager build output (root package + nested packages)
swift/**/.build/

# Haskell 16 章示例运行沙盒（运行时自建自删，异常中断可能残留）
haskell/examples/16_files/scratch16/

# Haskell stack 构建产物（Julia 的 Manifest 入库、stack 的 .stack-work 不入库）
**/.stack-work/

# OCaml 编译产物（ocamlc/ocamlopt 放在源文件旁；build/ 已被通用规则覆盖）
*.cmi
*.cmo
*.cmx
*.cmt
*.cmti

# Clojure 构建缓存（clojure CLI 的 .cpcache、REPL 端口文件；target/ 已被通用规则覆盖）
**/.cpcache/
**/.nrepl-port
**/.lein-repl-history
**/.lein-failures

# Algol 68：macOS 上 a68g -O2 的编译产物（Mach-O .so），曾在 examples/ 误入库
algol68/examples/**/*.so
