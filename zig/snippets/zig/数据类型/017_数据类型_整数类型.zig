pub fn main() void {
    var a: i8 = -123;
    var b: u32 = 456;
    var c: usize = 1000;

    // 进制表示
    var hex = 0xFF;      // 十六进制
    var oct = 0o77;      // 八进制
    var bin = 0b1010;    // 二进制
}
