pub fn main() void {
    const a: i8 = -123;
    const b: u32 = 456;
    const c: usize = 1000;

    // 进制表示
    const hex: u8 = 0xFF;      // 十六进制
    const oct: u8 = 0o77;      // 八进制
    const bin: u8 = 0b1010;    // 二进制

    _ = a;
    _ = b;
    _ = c;
    _ = hex;
    _ = oct;
    _ = bin;
}
