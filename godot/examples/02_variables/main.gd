extends SceneTree

func _initialize():
    var score := 100
    var player_name := "Alice"
    var is_ready := true
    var hp_ratio := 0.75

    print("player_name = ", player_name)
    print("score = ", score)
    print("is_ready = ", is_ready)
    print("hp_ratio = ", hp_ratio)
    quit()
