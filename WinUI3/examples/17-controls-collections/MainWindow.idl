// MainWindow.idl -- minimal: nothing here is x:Bind-bound, and event handlers wired
// by name in XAML (SelectionChanged="...") need no IDL entry.
namespace CollectionsGallery
{
    [default_interface]
    runtimeclass MainWindow : Microsoft.UI.Xaml.Window
    {
        MainWindow();
    }
}
