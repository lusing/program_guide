<Page
    x:Class="BasicGallery.NumberBoxPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Margin="24">
        <StackPanel Spacing="12" MaxWidth="480">
            <TextBlock x:Name="StatusText" Text="Ready" FontSize="18"/>

            <!-- 13.2 NumberBox：Value 是 double，空输入 = NaN；AcceptsExpression 吃算式 -->
            <NumberBox x:Name="Quantity" Header="Quantity" Value="1"
                       SmallChange="1" LargeChange="10"
                       SpinButtonPlacementMode="Compact"
                       AcceptsExpression="True"
                       ValueChanged="OnQuantityChanged"/>

            <!-- 13.3 程序化调值 + 解析路径演示 -->
            <StackPanel Orientation="Horizontal" Spacing="12">
                <Button Content="Double it" Click="OnDoubleClicked"/>
                <Button Content="Clear" Click="OnClearClicked"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>
