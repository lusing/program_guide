namespace SettingsHub
{
    [default_interface]
    runtimeclass App : Microsoft.UI.Xaml.Application
    {
        App();
        void ApplyTheme(String theme);   // "light" / "dark" / "default"
        void ShowSavedBar();              // green InfoBar feedback at the window bottom
    }
}
