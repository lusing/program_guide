# 这些文件必须保持 LF：bash 脚本在 WSL/Linux 里跑（CRLF 会报 bash\r），
# gforth 的 .fs 源码同理（CRLF 会把 \r 混进 token）。
*.sh text eol=lf
forth/**/*.fs text eol=lf
# SML 源码在 WSL 里跑三套编译器：CRLF 会混进注释/字符串比较，统一 LF。
sml/examples/*.sml text eol=lf
