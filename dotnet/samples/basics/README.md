# 基础语法示例

## Hello World
```bash
cd samples/basics/Hello
dotnet run
```

## 运行所有示例
```bash
# 逐个运行
cd samples/basics/Variables && dotnet run
cd samples/basics/ControlFlow && dotnet run
cd samples/basics/Methods && dotnet run
```