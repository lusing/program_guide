<Window x:Class="FileDialogDemo.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="File Dialog Demo" Height="260" Width="480">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="文件对话框示例" FontSize="22" FontWeight="Bold" Margin="0,0,0,12"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,12">
            <Button Content="打开文件" Click="OpenFile_Click" Width="120" Height="32" Margin="0,0,12,0"/>
            <Button Content="保存文件" Click="SaveFile_Click" Width="120" Height="32"/>
        </StackPanel>

        <TextBox x:Name="PathBox" Grid.Row="2" Text="未选择文件" TextWrapping="Wrap" IsReadOnly="True" VerticalScrollBarVisibility="Auto"/>
    </Grid>
</Window>
