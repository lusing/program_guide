<Window x:Class="TriggersDemo.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="触发器实验室" Height="520" Width="460">

    <Window.Resources>
        <!-- ① 属性触发器：聚焦/悬停时改外观 -->
        <Style x:Key="FocusHintTextBox" TargetType="TextBox">
            <Setter Property="Margin" Value="0,0,0,8"/>
            <Setter Property="Padding" Value="6,4"/>
            <Style.Triggers>
                <Trigger Property="IsKeyboardFocusWithin" Value="True">
                    <Setter Property="Background" Value="#FFFBEB"/>
                    <Setter Property="BorderBrush" Value="#F59E0B"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- ② 多条件触发器：悬停且可用时才加深 -->
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#3B82F6"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="14,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Style.Triggers>
                <MultiTrigger>
                    <MultiTrigger.Conditions>
                        <Condition Property="IsMouseOver" Value="True"/>
                        <Condition Property="IsEnabled" Value="True"/>
                    </MultiTrigger.Conditions>
                    <Setter Property="Background" Value="#1D4ED8"/>
                </MultiTrigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#CBD5E1"/>
                    <Setter Property="Foreground" Value="#64748B"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <!-- ④ 事件触发器：窗口载入时淡入（动画预告，第 20 章展开） -->
    <Window.Triggers>
        <EventTrigger RoutedEvent="Loaded">
            <BeginStoryboard>
                <Storyboard>
                    <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                     From="0" To="1" Duration="0:0:0.6"/>
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </Window.Triggers>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="16">

            <TextBlock Text="① 属性触发器：点击输入框获得焦点，背景与边框自动变色" FontWeight="Bold"
                       Margin="0,0,0,8"/>
            <TextBox Style="{StaticResource FocusHintTextBox}" Text="点我获得焦点试试"/>

            <TextBlock Text="② 多条件触发器：按钮「悬停 且 可用」才变深色" FontWeight="Bold"
                       Margin="0,8,0,8"/>
            <CheckBox x:Name="AgreeBox" Content="同意协议（勾选后按钮变为可用）" Margin="0,0,0,8"/>
            <Button Style="{StaticResource PrimaryButton}" Content="提交"
                    Width="120" HorizontalAlignment="Left"
                    IsEnabled="{Binding IsChecked, ElementName=AgreeBox}"/>

            <TextBlock Text="③ 数据触发器：CheckBox 的状态驱动整个面板换肤（零代码）" FontWeight="Bold"
                       Margin="0,16,0,8"/>
            <CheckBox x:Name="DarkBox" Content="夜间模式" Margin="0,0,0,8"/>
            <!-- 注意：Border 的底色写在 Style 的 Setter 里而不是元素上，
                 否则本地值优先级更高，DataTrigger 改不动（本章最重要的一课） -->
            <Border CornerRadius="8" Padding="16" Margin="0,0,0,8">
                <Border.Style>
                    <Style TargetType="Border">
                        <Setter Property="Background" Value="#F1F5F9"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsChecked, ElementName=DarkBox}" Value="True">
                                <Setter Property="Background" Value="#1E293B"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                <TextBlock Text="两个 DataTrigger（面板背景 + 文字颜色）同时响应一个状态">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Foreground" Value="#0F172A"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsChecked, ElementName=DarkBox}" Value="True">
                                    <Setter Property="Foreground" Value="#E2E8F0"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </Border>

            <TextBlock Text="④ 事件触发器：鼠标移入/移出，按钮宽度动画伸缩（动画预告）" FontWeight="Bold"
                       Margin="0,8,0,8"/>
            <Button Content="鼠标滑过我" Width="120" HorizontalAlignment="Left">
                <Button.Triggers>
                    <EventTrigger RoutedEvent="MouseEnter">
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="Width"
                                                 To="180" Duration="0:0:0.25"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger>
                    <EventTrigger RoutedEvent="MouseLeave">
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="Width"
                                                 To="120" Duration="0:0:0.25"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger>
                </Button.Triggers>
            </Button>

        </StackPanel>
    </ScrollViewer>
</Window>
