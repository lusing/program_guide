# 第 17 章测试输入：优化层前后行为必须一致

def unary! (x) if x < 1 then 1 else x * !(x - 1)

def unary- (v) 0 - v

def fib(n) if n < 2 then n else fib(n - 1) + fib(n - 2)

def sum_to(n) var acc = 0 in (for i = 1, n, 1 in acc = acc + i) * 0 + acc

print(fib(20))

print(!(10))

print(sum_to(1000))

fib(28)
